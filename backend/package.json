{
  "name": "backend",
  "version": "1.0.0",
  "description": "FastAPI backend with a LangGraph-based multi-agent workflow in `app/agents/agent1`.",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jest": "^30.3.0"
  }
}
