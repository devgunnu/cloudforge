Generate {{ language }} application code for the following AWS service.

{% if architecture_overview %}
## Architecture Overview
Use this to understand data flow and messaging patterns. DO NOT import or call code from other services — communicate only via HTTP/SDK/events as defined in the API Contracts below.

{{ architecture_overview }}
{% endif %}

## Service Details
- ID: {{ service_id }}
- Type: {{ service_type | upper }}
- Label: {{ label }}
{% if config %}
- Configuration:
{{ config | tojson(indent=2) }}
{% endif %}

{% if incoming or outgoing %}
## Connections
{% if incoming %}
This service **receives** from:
{% for conn in incoming %}
  - `{{ conn.from }}` (type: {{ conn.service_type | default("unknown") }}) via {{ conn.via }}
{% endfor %}
{% endif %}
{% if outgoing %}
This service **calls / triggers**:
{% for conn in outgoing %}
  - `{{ conn.to }}` (type: {{ conn.service_type | default("unknown") }}) via {{ conn.via }}
{% endfor %}
{% endif %}
{% endif %}

{% if api_contracts %}
## API Contracts
This service participates in the following integration contracts:
{% for c in api_contracts %}
### {{ c.source_service_id }} → {{ c.target_service_id }} ({{ c.contract_type }})
Payload schema:
{{ c.payload_schema | tojson(indent=2) }}
{% if c.function_signatures[language] is defined %}
Handler signature to implement on THIS service ({{ language }}): `{{ c.function_signatures[language] }}`
{# This is the interface contract — implement this signature in your handler. Do NOT import it from another service. #}
{% endif %}
{% if c.notes %}Notes: {{ c.notes }}{% endif %}
{% endfor %}
{% endif %}

{% if tf_context %}
## Relevant Terraform Configuration
The following Terraform resources are provisioned for this service — use the resource names/ARNs as environment variable references:
```hcl
{{ tf_context }}
```
{% endif %}

## Task
Generate the handler/entry point file: `services/{{ service_id }}/index.{{ ext }}`

{% if service_type == "lambda" %}
{% if language == "python" %}
Implement: `def handler(event: dict, context: Any) -> dict:`
{% elif language == "typescript" %}
Implement: `export const handler = async (event: APIGatewayProxyEvent): Promise<APIGatewayProxyResult> =>`
{% else %}
Implement the standard handler entry point for {{ language }}.
{% endif %}
{% elif service_type == "ecs" or service_type == "ec2" %}
Generate a long-running service entrypoint appropriate for a container or server process.
{% elif service_type == "glue" %}
Generate a PySpark/Glue ETL script with a main() function.
{% elif service_type == "step_functions" %}
Generate Lambda handler functions for each state in the state machine.
{% endif %}

Output ONLY the raw code content. No markdown. No explanation.
